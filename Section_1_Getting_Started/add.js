function add(num1, num2) {
    return num1 + num2;
}
//console.log(add("1", "2")); this will throw a compilation error, because we are sending chars when the inputs should be of type number
console.log(add(1, 2));
